
import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.verse3records.app',
  appName: 'Verse3 Records',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
