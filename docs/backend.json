
{
  "entities": {
    "Merchandise": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Merchandise",
      "type": "object",
      "description": "Represents a merchandise item for sale.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the merchandise item."
        },
        "name": {
          "type": "string",
          "description": "The name of the merchandise item."
        },
        "description": {
          "type": "string",
          "description": "A detailed description of the merchandise item."
        },
        "price": {
          "type": "number",
          "description": "The price of the merchandise item."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the image for the merchandise item.",
          "format": "uri"
        },
        "stockQuantity": {
          "type": "number",
          "description": "The number of items currently in stock."
        },
        "type": {
          "type": "string",
          "description": "The type of merchandise (e.g., 'record', 'EP', 'vinyl', 't-shirt')."
        }
      },
      "required": [
        "id",
        "name",
        "description",
        "price",
        "imageUrl",
        "stockQuantity",
        "type"
      ]
    },
    "MusicTrack": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "MusicTrack",
      "type": "object",
      "description": "Represents a music track uploaded to the platform.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the music track."
        },
        "artistId": {
          "type": "string",
          "description": "Reference to Artist. (Relationship: Artist 1:N MusicTrack)"
        },
        "title": {
          "type": "string",
          "description": "The title of the music track."
        },
        "audioUrl": {
          "type": "string",
          "description": "URL of the audio file for the music track.",
          "format": "uri"
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the image associated with the music track.",
          "format": "uri"
        },
        "uploadDateTime": {
          "type": "string",
          "description": "The date and time the music track was uploaded.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "artistId",
        "title",
        "audioUrl",
        "imageUrl",
        "uploadDateTime"
      ]
    },
    "Artist": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Artist",
      "type": "object",
      "description": "Represents an artist featured on Verse3 Records.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the artist."
        },
        "name": {
          "type": "string",
          "description": "The name of the artist."
        },
        "bio": {
          "type": "string",
          "description": "A biography of the artist."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the artist's image.",
          "format": "uri"
        },
        "genre": {
          "type": "string",
          "description": "The genre of music the artist performs."
        }
      },
      "required": [
        "id",
        "name",
        "bio",
        "imageUrl",
        "genre"
      ]
    },
    "NewsEvent": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "NewsEvent",
      "type": "object",
      "description": "Represents a news article or event announcement.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the news event."
        },
        "title": {
          "type": "string",
          "description": "The title of the news event."
        },
        "content": {
          "type": "string",
          "description": "The content of the news event (e.g., article text, event details)."
        },
        "publishDateTime": {
          "type": "string",
          "description": "The date and time the news event was published.",
          "format": "date-time"
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the image associated with the news event.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "title",
        "content",
        "publishDateTime"
      ]
    },
    "UserProfile": {
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Verse3 Records website.",
      "properties": {
        "email": {
          "type": "string",
          "description": "The email address of the user.",
          "format": "email"
        },
        "name": {
          "type": "string",
          "description": "The name of the user."
        }
      },
      "required": [
        "email",
        "name"
      ]
    },
    "DemoSubmission": {
      "title": "DemoSubmission",
      "type": "object",
      "description": "Represents a demo submission from an artist.",
      "properties": {
        "artistName": {
          "type": "string",
          "description": "The name of the artist submitting the demo."
        },
        "artistEmail": {
          "type": "string",
          "description": "The email of the artist.",
          "format": "email"
        },
        "trackTitle": {
          "type": "string",
          "description": "The title of the submitted track."
        },
        "audioUrl": {
          "type": "string",
          "description": "URL to the uploaded audio file in Firebase Storage.",
          "format": "uri"
        },
        "imageUrl": {
          "type": "string",
          "description": "URL to the optional uploaded image file in Firebase Storage.",
          "format": "uri"
        },
        "message": {
          "type": "string",
          "description": "An optional message from the artist."
        },
        "submittedAt": {
          "type": "string",
          "description": "The timestamp when the demo was submitted.",
          "format": "date-time"
        }
      },
      "required": [
        "artistName",
        "artistEmail",
        "trackTitle",
        "audioUrl",
        "submittedAt"
      ]
    },
    "PurchasedProduct": {
      "title": "Purchased Product",
      "type": "object",
      "description": "Represents a digital product purchased by a user.",
      "properties": {
        "productId": {
          "type": "string",
          "description": "The ID of the product that was purchased."
        },
        "productName": {
          "type": "string",
          "description": "The name of the product."
        },
        "downloadUrl": {
          "type": "string",
          "description": "The secure download URL for the purchased file in Firebase Storage.",
          "format": "uri"
        },
        "purchasedAt": {
          "type": "string",
          "description": "The timestamp when the product was purchased.",
          "format": "date-time"
        }
      },
      "required": [
        "productId",
        "productName",
        "downloadUrl",
        "purchasedAt"
      ]
    },
    "Product": {
      "title": "Product",
      "type": "object",
      "description": "Represents a product for sale in the store.",
      "properties": {
        "name": {
          "type": "string"
        },
        "slug": {
          "type": "string"
        },
        "price": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "imageUrl": {
          "type": "string",
          "format": "uri"
        },
        "revolutLink": {
          "type": "string",
          "format": "uri"
        },
        "type": {
          "type": "string",
          "enum": [
            "merch",
            "music"
          ]
        },
        "brand": {
          "type": "string",
          "enum": [
            "Verse 3 Merch",
            "Crude City"
          ]
        },
        "digital": {
          "type": "boolean"
        },
        "downloadUrl": {
          "type": "string",
          "format": "uri"
        },
        "sizes": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Available sizes for the merchandise (e.g., 'S', 'M', 'L', 'XL')."
        }
      },
      "required": [
        "name",
        "slug",
        "price",
        "description",
        "imageUrl",
        "revolutLink",
        "type",
        "brand"
      ]
    },
    "Order": {
      "title": "Order",
      "type": "object",
      "description": "Represents a customer order for physical goods.",
      "properties": {
        "customerDetails": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "email": { "type": "string", "format": "email" },
            "phone": { "type": "string" },
            "address": { "type": "string" }
          },
          "required": ["name", "email", "phone", "address"]
        },
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "name": { "type": "string" },
              "quantity": { "type": "number" },
              "price": { "type": "string" },
              "size": { "type": "string" }
            },
            "required": ["id", "name", "quantity", "price"]
          }
        },
        "total": { "type": "string" },
        "submittedAt": { "type": "string", "format": "date-time" },
        "status": { "type": "string" },
        "userId": { "type": "string" }
      },
      "required": ["customerDetails", "items", "total", "submittedAt", "status"]
    },
    "NewsletterSubscription": {
      "title": "Newsletter Subscription",
      "type": "object",
      "description": "Represents a newsletter signup email address.",
      "properties": {
        "email": {
          "type": "string",
          "format": "email"
        },
        "subscribedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["email", "subscribedAt"]
    }
  },
  "auth": {
    "providers": [
      "google.com",
      "password"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/UserProfile"
      },
      "description": "Stores user profile information, including name and email. Only the authenticated user can access their own profile."
    },
    "/demoSubmissions/{submissionId}": {
      "schema": {
        "$ref": "#/backend/entities/DemoSubmission"
      },
      "description": "Stores demo submissions from artists. Writeable by anyone, but not publicly readable to ensure privacy. Admins can view submissions in the Firebase Console."
    },
    "/users/{userId}/purchasedProducts/{purchaseId}": {
      "schema": {
        "$ref": "#/backend/entities/PurchasedProduct"
      },
      "description": "Stores a record of digital products a user has purchased. Only the user can read their own purchase history. Writes should be handled by a secure backend function after payment confirmation."
    },
    "/products/{productId}": {
      "schema": {
        "$ref": "#/backend/entities/Product"
      },
      "description": "Stores all products for the store. Publicly readable, but only writeable by admins."
    },
    "/orders/{orderId}": {
        "schema": {
            "$ref": "#/backend/entities/Order"
        },
        "description": "Stores customer orders. Writable by anyone to allow guest checkout, but only readable by admins."
    },
    "/newsletterSubscriptions/{subId}": {
      "schema": {
        "$ref": "#/backend/entities/NewsletterSubscription"
      },
      "description": "Stores email addresses for newsletter signups. Writable by anyone, readable only by admins."
    }
  }
}
